// script.js

// A simple script to log a message to the console
console.log("Hello, World!");

// Function to add two numbers
function addNumbers(a, b) {
    return a + b;
}

// Example usage
const result = addNumbers(5, 3);
console.log("The result is:", result);